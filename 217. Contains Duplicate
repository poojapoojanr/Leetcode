class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        # nums.sort()
        # for i in range(1,len(nums)):
        #     if nums[i]==nums[i-1]:
        #         return True
        
        # return False
        res = set()
        for n in nums:
            if n not in res:
                res.add(n)
            else:
                return True
        return False

        
