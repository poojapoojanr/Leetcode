from itertools import permutations
class Solution:
    def permuteUnique(self, nums: List[int]) -> List[List[int]]:
        res = []
        res = list(set(permutations(nums)))
        return list(res)
