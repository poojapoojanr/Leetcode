class Solution:
    def isPalindrome(self, s: str) -> bool:
        if len(s) <= 1:
            return True
        S1 = ''.join(char for char in s if char.isalnum())
        S1 = S1.lower()
        print(S1)
        return S1 == S1[::-1] 

